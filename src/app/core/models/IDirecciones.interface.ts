/**
 * Interfaz que define la estructura de datos para representar una dirección.
 */
export interface IDireccionInterface {
    id: number, // Identificador único de la dirección.
    direccion: string, // Dirección en formato de texto.
    url: string, // URL relacionada con la dirección
}