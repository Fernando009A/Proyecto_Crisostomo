import { TotalLotesPipe } from './total-lotes.pipe';

describe('TotalLotesPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalLotesPipe();
    expect(pipe).toBeTruthy();
  });
});
